{
  "name": "voe-client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
"scripts": {
  "dev": "vite",
  "build:client": "vite build",
  "build:ssr": "set VITE_SSR_BUILD=true&& vite build --ssr",
  "build": "npm run build:client && npm run build:ssr"
},
  "dependencies": {
    "axios": "^1.8.3",
    "pinia": "^3.0.2",
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "vue-the-mask": "^0.11.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "vite": "^6.2.0"
  }
}
